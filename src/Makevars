## Emacs please make this a -*- mode: Makefile; -*-

## For a package, just do this:
## PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"`
## And drop the RCPP_CPPFLAGS line.

GSL_CONFIG=gsl-config
GSL_CFLAGS=`${GSL_CONFIG} --cflags`
GSL_LIBS=`${GSL_CONFIG} --libs`

RCPP_CPPFLAGS=$(shell R --slave -e 'Rcpp:::CxxFlags()')
RCPP_LDFLAGS=$(shell R --slave -e 'Rcpp:::LdFlags()')

PKG_CPPFLAGS=$(RCPP_CPPFLAGS) $(GSL_CFLAGS)
PKG_LIBS=$(RCPP_LDFLAGS) $(GSL_LIBS)

## To enable profiling with Google Perftools:
# PKG_LIBS:=$(PKG_LIBS) -lprofiler
## However, Instruments seems to be working again.
