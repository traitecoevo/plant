# Sources for the plant vignettes

```r
install.packages("drat")
drat:::add("traitecoevo")
install.packages("remake")
remake::install_missing_packages()
remake::make()
```

This makes the standalone versions as well as package versions.
